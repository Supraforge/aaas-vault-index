# auto-generated by release_bootstrap — edit freely
version_source = "VERSION"
# test_command = "<none autodetected — set one; the gate fails closed>"
default_branch = "main"
release_workflow = "release.yml"
